<h1>Edit your profile</h1>

<div>
  <%= form_for @user do |f| %>
    <%= render partial: 'form', locals: {f: f} %>
    <p>
      <%= f.submit "Save" %>
    </p>
  <% end %>
</div>
<p class="back">
  <%= link_to "Cancel", user_path, {:class => "cancel"} %>
</p>
