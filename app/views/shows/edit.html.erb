<h1>Edit show</h1>

<div>
  <%= form_for [ @user, @show ] do |f| %>
    <%= render partial: 'form', locals: {f: f} %>
    <p>
      <%= f.submit "Save" %>
    </p>
  <% end %>
</div>
<p>
  <%= link_to "Cancel", :back %>
</p>
